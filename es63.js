//ITERABLES

//string and array are ITERABLES wears as object is not iterable


//string
const fName="dawa";

for(let char of fName) {
    console.log(char);
}

//array;
 
const items = ['dawa', 'sherpa'];
for(let name of items) {
    console.log(name);
}